type User {
    id: ID! @unique
    name: String!
    email: String!
}

type Subscription {
    id: ID! @unique
    planName: String!
    description: String
    createdAt: DateTime!
    updatedAt: DateTime!
    # user: User!
}
